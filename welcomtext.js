const welcome = (number, groupname) => {
    return `𝐎𝐥𝐚 @${number}. 𝐒𝐞𝐣𝐚 𝐛𝐞𝐦-𝐯𝐢𝐧𝐝𝐨 ${groupname}`
}
exports.welcome = welcome

const bye = (number) => {
    return `Adeus @${number}. espero nos vermos no inferno `
}
exports.bye = bye
